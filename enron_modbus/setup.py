from setuptools import setup

setup(
   name='enron-modbus',
   version='1.0',
   description='Modbus that includes 32 bit registers',
   author='u9n',
   author_email='foomail@foo.example',
   packages=['enron-modbus'],  #same as name
   install_requires=[], #external packages as dependencies
)
